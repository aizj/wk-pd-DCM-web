{
  "name": "vehicle-road-cloud-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @vrc/web",
    "build": "npm run build --workspace @vrc/contracts && npm run build --workspace @vrc/web",
    "typecheck": "npm run build --workspace @vrc/contracts && npm run typecheck --workspace @vrc/web && npm run typecheck --workspace @vrc/contracts",
    "test": "npm run test --workspace @vrc/web",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "check": "npm run check:boundaries && npm run typecheck && npm run test && npm run build"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
